service: the-marriage-of-ferris

provider:
  name: aws
  runtime: rust
  memorySize: 128
  iamRoleStatments:
    - Effect: "Allow"
      Action:
        - "logs:CreateLogGroup"
        - "logs:CreateLogStream"
        - "logs:PutLogEvents"
        - "logs:DescribeLogStreams"
      Resource:
        - "*"

plugins:
  - serverless-rust

package:
  individually: true

functions:
  test:
    handler: test
    events:
      - http:
          path: test
          method: get

  create_household:
    handler: create_household
    events:
      - http:
          path: household
          method: put